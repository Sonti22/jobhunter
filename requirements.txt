# jobhunter — зависимости

# HTTP
httpx>=0.28

# Данные
SQLAlchemy>=2.0

# Конфиг
pydantic>=2.5
pydantic-settings>=2.1
PyYAML>=6.0

# Резюме
reportlab>=4.0,<6
pypdf>=4.0

# Часовые пояса (Windows не имеет системной tz-базы)
tzdata>=2024.1

# TLS-пиннинг
cryptography>=42.0

# Планировщик
APScheduler>=3.10

# Дашборд
fastapi>=0.109
uvicorn[standard]>=0.27
# Формы дашборда (кнопки «одобрить» и «стоп»). На машине разработчика
# приезжает транзитивно, в чистом образе — нет, и FastAPI падает на старте.
python-multipart>=0.0.9

# Тесты. Держим в основном списке намеренно: проверять надо тот же образ,
# который работает, а не похожий. Весит мало, в рантайме не участвует.
pytest>=8.0

# Google Calendar: подтверждённое интервью уезжает в календарь владельца.
# Без этих библиотек schedule/gcal.py молча откатывается на .ics-файл.
google-api-python-client>=2.100
google-auth-oauthlib>=1.2

# Telegram. MTProto (личный аккаунт) — отклики и переписка с рекрутёрами.
# Бот от BotFather ходит в api.telegram.org через httpx выше: отдельная
# библиотека вроде aiogram не нужна — один чат и десяток экранов.
telethon>=1.44,<2
cryptg>=0.4            # нативное ускорение шифрования MTProto
