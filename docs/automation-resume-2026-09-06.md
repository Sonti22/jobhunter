# Возобновление авторежима — 6 сентября 2026

По явному запросу владельца «давай включим автоматику» в 20:43:50 МСК
изменено только `campaign_state.manual_only`: `1 -> 0` в рабочей базе.
Код, образы, расписание и SMTP не менялись. Сервисы не перезапускались.

## Сохранённые ограничения

- Лок `cold_only` до `2026-09-07 13:53:19.994560 UTC`
  (7 сентября, 16:53:19 МСК) сохранён без изменений.
- История четырёх PeerFlood, потолок 12 и все строки дневных квот сохранены.
- Пять записей `manual_tg_*` остались ручными, включая одну пропущенную.
- 29 неоднозначных отправок не возвращались в очередь.
- Автоответы в обычных, не ручных диалогах снова разрешены существующей
  политикой. Разрешение не означает, что ответ уже отправлен.
- Статус антиспам-ограничений самого аккаунта Telegram не проверен.
  Изменение настройки проекта не снимает ограничения Telegram.

## Проверки и резервная копия

- Перед изменением создана SQLite-копия с `integrity_check=ok` и проверкой
  внешних ключей: `backup/jobhunter-before-auto-20260906-2045.db`.
- SHA-256: `6e1a4e3e33d8131fd45c6f52a7a0d1e122b8e80833fef35775437876f2e6d200`.
- На копии в Docker с `--network none` проверено: после переключения
  первые сообщения остаются заблокированы действующим локом; все пять
  ручных записей и 29 неоднозначных отправок исключены из автоотправки.
- На момент проверки в копии было 75 допустимых кандидатов Telegram.
  Это не число отправленных сообщений; при отправке условия проверяются снова.
- Изменение выполнено одной транзакцией с проверками ожидаемых состояния,
  лока и отсутствия `SENDING`; подтверждено, что изменён ровно один флаг.
- После изменения рабочая политика: `cold_allowed=false`,
  `warm_reply_allowed=true`; все три сервиса healthy; `/sending` отвечает 200.
- Число записей журнала отправки до и после изменения: 151.
  Проверочные сообщения не отправлялись; второй Telegram-клиент не запускался.

Текущее расписание первых сообщений — 11:15 и 16:40 МСК. Оба окна
7 сентября предшествуют окончанию лока. При неизменном расписании ближайшее
окно после него — 8 сентября, 11:15 МСК; фактическая отправка зависит от
повторных проверок очереди, квот и ответа Telegram.

При необходимости остановки использовать штатный стоп-кран. Не очищать
PeerFlood, таймеры или историю доставки и не восстанавливать всю старую базу
поверх новых переписок ради изменения одного режима.
