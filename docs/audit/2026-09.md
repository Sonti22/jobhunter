# Проверка бота, сентябрь 2026

Как читать: каждая находка — с доказательством из базы или живого сервиса, оценкой пользы
(сколько откликов, ответов или минут владельца она возвращает) и трудоёмкости
(S — до часа, M — полдня, L — день и больше). Исправления — после выбора владельца.

Точка отсчёта (18.09): 20 825 вакансий → 101 отправленный отклик → ~29 ответов → 0 интервью.

---

## A1. Воронка и «мёртвые» очереди

Снято 18.09 скриптом `audit_a1.py` (только чтение).

### 1.1. Бот неделями почти ничего не отправляет
Отправки за 14 дней по дням: 05.09 — 11, 06.09 — 4, 07.09 — 1, 09.09 — 9, 10.09 — 1,
**11–15.09 — ноль пять дней подряд**, 16.09 — 5, 17.09 — 16, 18.09 — 3.
При потолке почты 45 в день реально уходит 0–10. Причина не в лимитах, а в притоке: вакансий
с почтовым адресом мало, Telegram закрыт.
**Польза: главная.** Всё остальное в этом отчёте — про то, как поднять это число. **Труд: —**

### 1.2. Тридцать писем в статусе «доставка неизвестна»
06.09 — 21 и 09.09 — 9 отправок записаны как `ambiguous`: бот не знает, ушли они или нет.
Если ушли — ответы на них могут не привязываться к заявкам; если нет — 30 откликов потеряны.
Разобрать на этапе A5 (сверить с папкой «Отправленные» Gmail).
**Польза: до 30 откликов. Труд: S.**

### 1.3. Гейт режет письма за обрезанный заголовок — потеря по собственной вине
Из 89 заявок `GATE_FAILED` 22 упали на «непарные скобки или кавычки». Образцы заголовков:
`Tech Lead Аналитик КХД (обязателен банковский опыт от 5 лет, 7+ лет оп`,
`Full-Stack Engineer (full-time, REMOTE) + PhD Researcher (part-time, R`.
Заголовок обрезается по длине посреди скобки, письмо его цитирует — и не проходит проверку.
Ещё 14 упали на «цитата требования пересказана». Итого 36 из 89 (40%) — не про правду фактов,
а про оформление текста.
Лечение: в `tailor/roletitle.clean_title` отбрасывать незакрытый хвост в скобках.
**Польза: ~25% отказов гейта возвращаются в работу. Труд: S.**

### 1.4. Автопоиск каналов натаскал нерелевантных
В очереди на обработку — «Копирайтер», «HRD», «Директор по персоналу», «из города Верхняя
Пышма» (каналы `mvtopvacancy`, `workfortop`, `rabota_sredneur`, `udafrii`); в закрытых —
`vahta_ohrana` (95 постов), `obshepit_moscow` (113). Они не дают ни одного отклика, но сбор
из-за них идёт 45 минут (18.09 закончился в 11:06 МСК) и сдвигает всю утреннюю цепочку.
Лечение: отключать канал, если последние 50 его вакансий не дали ни одной рекомендованной.
**Польза: сбор в разы быстрее, чище база и счётчики. Труд: M.**

### 1.5. Каналы, где контакт теряется в 95–100% постов
Тот же рисунок, что был у «Рефералок в IT» (там терялись все 77 постов):

| Канал | Без контакта |
|---|---|
| `progjob` | 290 из 290 |
| `ya_jobs` | 130 из 130 |
| `pythonpythonjobs` | 117 из 123 |
| `remote_devops_jobs` | 83 из 85 |
| `remotejobss` | 84 из 87 |
| `jaabz_com` | 72 из 72 |
| `hiddengurus` | 142 из 172 |
| `remocate` | 97 из 109 |
| `vacanciesbest` | 89 из 93 |

Скорее всего, отклик там — ссылкой на сайт или форму. Разборщик теперь ловит кнопки ботов,
но обычную ссылку «откликнуться» контактом не считает, и вакансия закрывается как
недостижимая, не попадая даже в ручную очередь. Проверяется на этапе A2.
**Польза: сотни профильных вакансий (python, devops, remote) в ручную очередь. Труд: M.**

### 1.6. «Одобрено: 187» — на деле почти всё мертво
112 из 187 старше 14 дней, 146 — телеграм-контакты (канал закрыт). Из 62 почтовых к отправке
готовы только 17 прямых писем; остальные — устарели, ссылка вместо адреса, служебный ящик,
повтор тому же работодателю. Счётчик в боте вводит в заблуждение.
Лечение: автозакрытие с причиной; телеграм-заявки — в список ручной отправки.
**Польза: правдивые цифры, меньше шума. Труд: S.**

### 1.7. Ручная очередь: 1 153 свежих подходящих вакансии, разобрано 154 за всё время
Всего 6 584, с баллом ≥45 — 2 739, из них свежих (≤14 дней) — 1 153. По источникам свежих:
arbeitnow 500, ATS 165, careered 144, workable 113, WWR 94, himalayas 61, jobicy 35, habr 22.
Владелец физически закрывает 10–15 в день, значит решает не объём, а порядок выдачи.
Подозрение: 500 вакансий arbeitnow (немецкая доска) — в основном офис в Германии; проверить
на этапе A3, не раздувают ли они очередь.
**Польза: владелец тратит время на лучшие 15, а не на случайные. Труд: M.**

### 1.8. Вечерний сбор ждёт утра
Сбор из Telegram идёт трижды в день (09:30, 13:30, 18:30 МСК), а подготовка, одобрение и
почта — один раз утром. Всё собранное днём и вечером ждёт до 10:00 следующего дня и стареет
на сутки при допустимом возрасте вакансии 7 дней.
Лечение: после дневного и вечернего сбора запускать ту же цепочку
«подготовка → одобрение → почта» (отправщик повторов не делает).
**Польза: отклик в день публикации — выше шанс ответа. Труд: S.**

---

## A2. Источники вакансий

Снято 18.09 скриптами `audit_a2.py` (база) и `audit_a2_live.py` (живая проверка девяти каналов).

Отдача источников за 14 дней:

| Источник | Вакансий | С прямым контактом | Из них почта | Годных (балл ≥45) | Отправлено |
|---|---|---|---|---|---|
| Telegram-каналы | 11 600 | 5 440 | 1 215 | 1 528 | 27 |
| arbeitnow | 1 422 | 1 | 1 | 482 | 0 |
| careered | 1 083 | 13 | 0 | 437 | 0 |
| ATS-фиды | 774 | 0 | 0 | 463 | 0 |
| Hacker News | 263 | 92 | 92 | 59 | 18 |
| workable | 197 | 0 | 0 | 113 | 0 |
| WWR | 180 | 19 | 19 | 106 | 0 |
| himalayas | 151 | 0 | 0 | 58 | 0 |
| jobicy | 120 | 11 | 11 | 85 | 2 |
| habr | 113 | 0 | 0 | 19 | 0 |
| trudvsem | 112 | 75 | 75 | 35 | 7 |
| ergodotisi | 27 | 27 | 27 | 6 | 0 |

### 2.1. Ссылка «откликнуться на сайте» не считается контактом — вакансия выбрасывается
Живая проверка каналов, где контакт «терялся» в 95–100% постов:

| Канал | Вакансий на странице | Куда ведёт отклик |
|---|---|---|
| `progjob` | 15 | career.habr.com — все 15 |
| `ya_jobs` | 15 | yandex.ru (вакансии Яндекса) — все 15 |
| `jaabz_com` | 18 | jaabz.com — все 18 |
| `remote_devops_jobs` | 20 | weworkremotely, workingnomads, euremotejobs… — все 20 |
| `vacanciesbest` | 14 | facancy.ru — 13 |
| `hiddengurus` | 15 | geekjob.ru, team.vk.company, 01.tech… — 12 |
| `pythonpythonjobs` | 14 | youngjunior.ru — 10 |
| `remocate` | 12 | remocate.app — 10 |
| `remotejobss` | 20 | ссылок нет вовсе — канал бесполезен |

Для вакансий с досок (`arbeitnow`, ATS) ссылка на форму уже считается контактом и ведёт в
ручную очередь; для постов из Telegram — нет, они закрываются как «недостижимые» и даже не
оцениваются. Только по этим девяти каналам так потеряно ~1 100 вакансий, включая
бэкенд с Хабр Карьеры и вакансии Яндекса.
Лечение: в `ingest/tgchannels.py` брать последнюю внешнюю ссылку поста как
`ContactKind.EXTERNAL_URL` (по образцу кнопки бота, сделанной 18.09) — вакансия попадёт в
ручную очередь с кнопкой «Открыть вакансию». Уже закрытые — перечитать и вернуть.
**Польза: +сотни профильных вакансий в ручную очередь. Труд: M.**

### 2.2. Каналов 290, из них минимум полтора десятка — мусор
У 46 каналов ≥40 постов и ни одной годной вакансии (3 409 постов). Часть — жертвы
находки 2.1 (их вакансии просто не оценивались: `progjob`, `ya_jobs`, `remocate`), но часть —
чистый мусор автопоиска: `obshepit_moscow`, `vahta_ohrana`, `restorhoreca`, `mos_rabotka`,
`businessjur`, `junior_designers`, `frilans_na_legke`, `hrorhr`.
Лечение: после правки 2.1 пересчитать; каналы, где из последних 50 оценённых вакансий нет ни
одной годной, отключать автоматически и показывать владельцу списком.
**Польза: сбор быстрее, база и счётчики чище. Труд: M.**

### 2.3. Telegram даёт 1 215 вакансий с почтой за 14 дней, а писем ушло единицы
Это самый большой неиспользуемый запас: вакансия с адресом в посте может уйти почтой, хотя
Telegram закрыт. Почему не уходят — проверяется на этапе A3 (отбор): сколько из 1 215 отсеяно
по баллу, сколько — служебные ящики, сколько устарело, сколько ждёт одобрения.
**Польза: потенциально десятки писем в неделю. Труд: зависит от A3.**

### 2.4. trudvsem и ergodotisi — источники без отдачи
`trudvsem`: 75 вакансий с почтой, 7 отправлено, ответов 0 из 12 за всё время (госпортал,
офисные вакансии). `ergodotisi` (Кипр): 27 из 27 отсеяны по баллу.
Лечение: отключить отправку по `trudvsem`, источник `ergodotisi` выключить.
**Польза: не тратим дневной потолок почты и репутацию ящика впустую. Труд: S.**

### 2.5. Доски без контактов раздувают ручную очередь
`arbeitnow` — 1 422 вакансии, 482 «годных», контактов нет; это немецкая доска, большая часть —
офис в Германии. Вместе с ATS, careered и workable они дают ~1 500 «годных» вакансий в ручную
очередь при пропускной способности владельца 10–15 в день. Проверить на этапе A3, насколько
честно определяется удалённый формат, и ранжировать очередь жёстче.
**Польза: владелец видит лучшие 15, а не случайные. Труд: M.**

---

## A6. Входящие и диалоги — почему ответы не превращаются в интервью

Снято 19.09 скриптом `audit_a6.py`: 47 входящих сообщений, 32 диалога с ответом работодателя.
Этап поднят раньше A3–A5 как самый ценный.

### 6.1. Половина карточек владельцу истекает без ответа — главная причина нуля интервью
Из 25 карточек: **12 истекли**, 5 — «пропустить», 4 — «отправить», 4 ждут сейчас.
Задержка решения: медиана 24 часа — это и есть срок жизни карточки, то есть типичный исход —
«никто не ответил». Так потеряны живые разговоры:
GDL IT (рекрутёр задал вопросы о последнем проекте), True Engineering (уточняли роль),
«Присылайте резюме» от 25.08, ещё девять.
Бот делает трудную часть — получает ответ рекрутёра, — а дальше ответ умирает в ожидании.
Лечение: (а) напоминание за 3 часа до истечения и повтор утром; (б) ежедневная сводка «кто
ждёт твоего ответа» первым сообщением дня; (в) срок жизни карточки 72 часа и для Telegram
(у почты уже так); (г) для типовых реплик — кнопки готовых ответов в один тап.
**Польза: самая большая в отчёте — каждый второй ответ рекрутёра сейчас пропадает. Труд: M.**

### 6.2. Классификатор не понимает половину сообщений, включая простое «пришлите резюме»
24 из 47 входящих (51%) получили метку `unknown`. Среди них:
«Добрый вечер! Присылайте резюме», «Открыта, могу попросить вас резюме?», «можете скинуть
резюме!», «Если готовы рассмотреть, могу запросить резюме?». Каждое такое сообщение вместо
автоответа с резюме уходит карточкой владельцу — и с вероятностью 50% истекает (6.1).
Второе мнение LLM есть, и оно право: не согласилось с регэксом в 10 случаях из 13
(`unknown→ask_cv` ×3, `unknown→ack` ×2, `unknown→rejection` ×2), но проверено всего 13
сообщений из 47, и на решение оно не влияет.
Лечение: дополнить шаблоны `ask_cv` живыми формулировками из базы; при `unknown` у регэкса
и уверенном `ask_cv` у LLM — действовать по LLM (просьба прислать резюме безопасна).
**Польза: просьбы о резюме закрываются автоматически за минуты. Труд: S–M.**

### 6.3. Отказы и «вакансия на паузе» размечаются как согласие или предложение времени
«Набор приостановили по внутренним причинам» → `slot_proposed`; «временно на паузе, много
кандидатов» → `slot_proposed`; «вакансия уже не актуальна» → `ack`; «остановили поиск на
данную вакансию» → `ack`, и бот отправил на отказ автоответ-благодарность.
Вреда мало, но статусы врут: такие заявки висят как «в диалоге», а не закрыты.
Лечение: шаблоны отказа — «приостановили», «на паузе», «на стопе», «захолдили», «не актуальна»,
«остановили поиск», «уже выбрал кандидата».
**Польза: честная воронка, нет неловких автоответов. Труд: S.**

### 6.4. Решение владельца «отправить» не исполняется в ручном режиме Telegram
2 карточки: владелец нажал «отправить», бот отказал — «ручной режим после двух PeerFlood».
Владелец считает, что ответил, а рекрутёр ничего не получил.
Лечение: в ручном режиме карточка сразу даёт текст для копирования и кнопку-ссылку на чат
с рекрутёром, а после — кнопку «отправил сам».
**Польза: ответы владельца перестают теряться. Труд: S.**

### 6.5. ИСПРАВЛЕНО 19.09 — зависший ответ рекрутёра без карточки
Teamlead DevOps, заявка #6676: 8.09 рекрутёр написал «Пришлите свое резюме на рассмотрение».
Автоответ упёрся в ручной режим Telegram, сообщение 11 дней висело «в обработке», карточки
не было. Повторная обработка зависших сообщений существовала только для почтовых вакансий.
Исправление: `convo/inbox_email.retry_stuck` берёт и телеграм-ответы, пока Telegram в ручном
режиме (тогда повтор ничего не отправляет, а создаёт карточку). Тест:
`test_stuck_telegram_reply_becomes_card_while_telegram_is_manual_only`.

### 6.6. Бот трижды написал соискателям, приняв их резюме за вакансии
Заявки #4162, #4431, #7386 (25.08–05.09): «это не вакансия, это моё резюме», «я работу ищу, а
не сотрудника». После 05.09 таких случаев нет — фильтр `ingest/postkind.py` с тех пор усилен.
Держать под наблюдением; в ежедневную сводку — счётчик ответов с такими словами.
**Польза: репутация. Труд: S.**

### 6.7. Сейчас ждут владельца — действовать сегодня
| Заявка | Кто | Что нужно | Истекает (МСК) |
|---|---|---|---|
| #18000 | Python Developer Senior (Top Selection) | прислали условия сотрудничества, ждут ответа | 19.09 16:01 |
| #8699 | Astoria AI | «potential fit», описаны следующие шаги | 19.09 19:45 |
| #9368 | Search Atlas | подать по ссылке Teamtailor | 19.09 19:45 |
| #16064 | Zapier | подать через страницу вакансий | 20.09 17:05 |
| #6676 | Teamlead DevOps | просили резюме ещё 8.09 — карточка придёт утром | — |

---

## A8. Надёжность — находка вне очереди (19.09)

### 8.1. ИСПРАВЛЕНО — после обрыва сети автопилот зависает и сам не поднимается
19.09 с 11:52 до ~14:30 МСК у контейнера не было сети (DNS не резолвился: Gmail, Telegram,
careered, все LLM-провайдеры). Вызов Telethon завис без таймаута, шаги «входящие» и «решения»
остались «выполняющимися» навсегда, цепочка догона встала на сборе. Контейнер получил статус
`unhealthy`, но Docker такие контейнеры не перезапускает. Итог: за день не отработал ни один
шаг, первые пять прямых писем в 10:30 не ушли, отправок — ноль; автопилот простоял бы так до
ручного перезапуска.
Исправление: сторож `autopilot._watchdog` в отдельном пуле пульса — если телеграм-очередь
молчит дольше часа, процесс завершается, `restart: unless-stopped` поднимает контейнер, догон
на старте возвращает день. Тесты: `tests/test_autopilot_watchdog.py`.

### 8.2. Владелец не узнаёт, что бот стоит
Три часа простоя — и ни одного сигнала. Уведомление из самого автопилота при обрыве сети
невозможно, но контейнер `bot` живёт отдельно и видит пульс (`health.ages`).
Лечение: бот раз в 10 минут смотрит пульс автопилота и пишет владельцу «автопилот молчит
N минут», а после восстановления — что догнано и что нет.
**Польза: простой замечается за минуты, а не случайно. Труд: S.**

### 8.3. Сетевые вызовы без таймаута
Зависание — следствие вызова без предела по времени. Сторож лечит последствие, не причину.
Лечение: общий таймаут на шаги, открывающие Telethon (`asyncio.wait_for`), и на сбор по
источнику.
**Польза: шаг падает с ошибкой за минуты и не блокирует очередь. Труд: M.**

### 8.4. Догон не возвращает второй проход почты
В списке догоняемых шагов нет `email_late` (13:00): если машина проснулась после 13:00,
отправка пройдёт один раз в цепочке — этого достаточно, но стоит учесть при переходе на
цепочку «одобрение → почта» (находка 1.8).
**Труд: S.**

---

## Что исправлено 19.09 (после команды владельца «исправь все недочёты»)

| Находка | Что сделано | Где |
|---|---|---|
| 6.1 Карточки истекают без ответа | Напоминание через 8 ч, ещё одно за 3 ч до истечения, утренняя сводка «кто ждёт ответа»; ночью не будим; карточка «нужен человек» живёт 48 ч вместо 24; истечение и напоминания больше не зависят от Telegram | `owner.remind_pending`, `autopilot.step_interview_reminders` |
| 6.2 Классификатор слеп к «пришлите резюме» | Живые формулировки из базы: «могу запросить/попросить резюме», «можете скинуть/направить резюме». Подъём `unknown → ask_cv` по мнению LLM в движке уже был | `convo/classify.py` |
| 6.3 Отказы размечались как согласие | «Закрыли позицию», «захолдили», «на холде», «уже выбрал кандидата», «это не вакансия, это моё резюме», «я работу ищу» | `convo/classify.py` |
| 6.4 «Отправить» не исполнялось в ручном режиме Telegram | Вместо вечного «отложено» владелец получает текст и кнопку, открывающую чат с готовым ответом; карточка закрывается | `decisions._hand_over_manual_reply` |
| 6.5 Зависший ответ без карточки | Повтор зависших сообщений берёт и Telegram, пока он в ручном режиме | `convo/inbox_email.retry_stuck` |
| 1.3 Гейт бракует письма за скобку | Причина была не в заголовке, а в цитате требования, обрезанной посреди скобки; незакрытый хвост отбрасывается. 4 свежих письма возвращены на пересборку | `tailor/message._clip` |
| 2.1 Ссылка «откликнуться на сайте» не контакт | Последняя внешняя ссылка поста = `external_url` → ручная очередь; вакансия, закрытая как недостижимая, возвращается при повторном сборе | `ingest/tgchannels.apply_site_link`, `ingest/base.save_jobs` |
| 1.8 Вечерний сбор ждёт утра | После сбора в 13:30 и 18:30 сразу идёт цепочка подготовка → одобрение → почта | `autopilot._ingest_tg_then_send` |
| 2.4 Источники без отдачи | `DISABLED_SOURCES=trudvsem,ergodotisi`: не собираются и не тратят потолок почты | `config`, `autopilot`, `mailer.pick_batch` |
| 1.6 «Одобрено 187» — мёртвый хвост | Ежедневная уборка с причиной. Применено: 186 → 96 (72 устарели, 4 служебных ящика, 14 ушли в ручную очередь) | `repair_queue.sweep_dead_approved` |
| 8.1 Автопилот зависает после обрыва сети | Сторож завершает процесс, Docker поднимает, догон возвращает день | `autopilot._watchdog` |
| 8.2 Владелец не узнаёт о простое | Бот следит за пульсом автопилота и пишет «стоит» и «снова работает» | `bot/watch.py` |
| 1.2 Тридцать писем «доставка неизвестна» | Сверено с Gmail: 15 адресов из 21 письмо получили, дублей нет (у всех либо стоит время отправки, либо заявка закрыта). Три письма с Hacker News (civtiq, puma.tech, dualentry) не ушли ни разу — возвращены на пересборку | разбор + данные |
| — Сброс прямых писем каждый день | «Исправление языка» не трогает `direct:*` | `repair_queue.reset_email_language` |

## Что осталось

- **2.2 / 1.4** Автоотключение мусорных каналов (общепит, вахта, юристы) — труд M. После
  правки 2.1 часть «пустых» каналов оживёт, поэтому считать надо заново через 2–3 дня.
- **1.7 / 2.5** Порядок выдачи в ручной очереди (1 153 свежих вакансии на 10–15 в день).
- **8.3** Общий таймаут на шаги с Telethon — сейчас последствия лечит сторож, причина жива.
- **6.6** Счётчик ответов «это не вакансия» в ежедневной сводке.
- Этапы проверки **A3** (отбор), **A4** (письма и резюме), **A5** (отправка), **A7** (пульт),
  **A9** (безопасность), **A10** (код) ещё не пройдены.

---

## Идеи из открытых проектов на GitHub (просмотрено 20.09)

Смотрел описания, код не копировал: у проектов другой стек (TypeScript/JavaScript), а у
`job-ops` лицензия не определена. Переносим идеи, реализуем у себя.

| Проект | Звёзд | Что у них есть, чего нет у нас |
|---|---|---|
| [career-ops-hq/career-ops](https://github.com/career-ops-hq/career-ops) (MIT) | 72 тыс. | поиск недавно профинансированных компаний; детектор «вакансий-призраков» (перепубликации); анализ причин отказов и конверсии по каналам; расписание нескольких напоминаний |
| [DaKheera47/job-ops](https://github.com/DaKheera47/job-ops) | 3,9 тыс. | Gmail сам отмечает исход **любой** заявки: «приглашаем на интервью» → интервью, «к сожалению» → отказ |
| [Gsync/jobsync](https://github.com/Gsync/jobsync) (MIT) | 1,3 тыс. | панель активности по дням; страница компании (все её вакансии, контакты, история); встроенные каталоги компаний на Lever (1 160) и Ashby (1 860); MCP-сервер для вопросов из Claude |

Общее у всех трёх — тот же принцип, что и у нас: анкеты на сайтах за человека не заполняются,
последнее слово за владельцем.

### Что сделано сразу
**График «сколько сообщений уходит по дням»** на странице `/stats`: отклики почтой, прямые
письма и Telegram стопкой по дням за 30 дней, плитки «сегодня / 7 дней / 30 дней / ответы /
сбои», отдельные графики ответов рекрутёров и ручных откликов, таблица-дублёр.
График сразу показал две вещи: пять пустых дней 11–15.09 и то, что **ручные отклики
прекратились после 08.09** — до этого их было по 8–16 в день.

### Что стоит взять дальше (по убыванию пользы для автоматизации)
1. **Gmail отмечает исход ручных откликов** (идея job-ops). Сейчас почта разбирает только ответы
   на наши письма, а автоответы ATS на поданные вручную анкеты («Thank you for applying»,
   «Unfortunately…», «We'd like to invite you») пропускаются — 13 писем за один проход 19.09.
   Привязка по названию компании и домену ATS даст исход без единого действия владельца и
   честную воронку по ручным откликам. **Труд: M.**
2. **Недавно профинансированные компании как цели прямых писем** (идея career-ops). Канал
   прямых писем упирается в нехватку именных адресов; стартап после раунда нанимает, а его
   основатели чаще публикуют контакты. Источники — только открытые ленты. **Труд: M.**
3. **Детектор перепубликаций.** Вакансия, которая выходит заново каждую неделю, — либо
   «призрак», либо текучка; понижать ей балл и помечать в карточке. **Труд: S.**
4. **Второе напоминание рекрутёру** (сейчас одно, через 7 дней) — с проверкой по нашим же
   данным, даёт ли оно ответы. **Труд: S.**
5. **Страница компании** в пульте: вакансии, контакты, переписка, «уже писали тогда-то» —
   особенно полезна для прямых писем. **Труд: M.**
6. **Сводка недели в бот** теми же цифрами, что на графике. **Труд: S.**

Каталоги компаний Lever/Ashby брать пока незачем: они дают вакансии с анкетой, а ручная
очередь и так переполнена (1 153 свежих на 10–15 в день).

## Telegram: ручной режим и темп холодных (22–23.09)

**Ручной режим было нечем снять** (`3c97589`). После второго PeerFlood кампания уходила в
`manual_only` с обещанием «до твоего решения», но ни кнопки, ни команды для решения не было:
временный лок Telegram истёк 20.09, а наш самозапрет висел бы вечно. Добавлена кнопка
«▶️ Возобновить Telegram» на пульте — видна только в ручном режиме; потолок и история
страйков при снятии не сбрасываются, новый PeerFlood снова ставит ручной режим. Снят 22.09
по решению владельца.

**Темп: пауза вместо дневного потолка** (`0caa3fc`, `e75b1e0`, `d483fcd`). Причина 6 страйков —
плотность: внутри сессии сообщения уходили с паузой 1–5 минут, и сама пачка выглядела как спам
независимо от текста. Решение владельца 23.09: дневного потолка нет, новому адресату — не чаще
раза в 30 минут; тем, кто уже ответил, — без паузы (свой бакет 80/день).

- Метка последней холодной попытки лежит в базе (`campaign_state.last_cold_sent_at`): прогон
  запускается планировщиком заново, память процесса перезапуска не переживает.
- Пауза считается от **попытки**, а не только от успеха: отказ по приватности или обрыв посреди
  запроса — тоже сообщение незнакомцу.
- Прогон не спит между сообщениями: 30 минут сна заняли бы однопоточную tg-очередь, пульс
  `autopilot_tg` (порог 40 минут) не отбился бы, и сторож убил бы контейнер посреди отправки.
  Отправил одно — вернул `MORE_TO_SEND`, планировщик зовёт снова через 30–45 минут; каждое
  переназначение сохраняется в базе и переживает перезапуск.
- После пропуска (мёртвый ник) — пауза 20–60 с: серия резолвов подряд сама ловит FloodWait.
- `close_day()` вызывался и из отправщика, и из итога дня — счёт «чистых дней» удваивался.
- Пульт, веб и сводки показывали «N/потолок» — после отмены потолка это читалось бы как
  «осталось столько-то». Теперь — счёт за день и когда можно следующее; пауза показана как
  штатное ожидание, а не как сбой.

Цепочка дня: `tg1` в 11:15 МСК → по одному сообщению раз в 30–45 минут → окно закрывается в
21:00. Естественный предел — около 17–19 холодных в день.

Что сознательно оставлено: `quota_ceiling` и «разведка тремя сообщениями после лока» больше
ничего не ограничивают — это тоже дневные счётчики, снятые решением владельца. Любой новый
PeerFlood при 6 страйках сразу возвращает ручной режим.

### 23.09: обрыв сети и первая попытка после снятия ручного режима

**Сеть пропала с 10:20 до 12:08 МСК** — у контейнеров не резолвились ни Google, ни Telegram.
Бот 9 раз перезапускался в ожидании сети; сбор вакансий дождался её и набрал 2 000+ новых;
новых сбоев отправки не было. Найдены и исправлены две дыры:

- **Telegram молчал бы до 16:40** (`9a195d3`). tg1 в 11:15 упал на подключении, а догон шаг не
  повторил: он срабатывает на старте и после сна ПК, не после обрыва. Теперь сетевая ошибка
  (`ConnectionError`, `TimeoutError`, `gaierror`) переназначает цепочку через 15 минут;
  ошибки кода не повторяются.
- **Карточка истекла, пока бот был без связи** (`2a5fe8a`). #28 закрылась в 11:07, напоминание
  «истекает через 1 ч» пришло владельцу в 12:08. Теперь при протухшем пульсе бота или висящих
  уведомлениях срок сдвигается на «сейчас + 2 ч».

**PeerFlood на первом же сообщении** (12:46, после снятия ручного режима 22.09 и возврата сети).
Аккаунт по-прежнему под антиспам-ограничением Telegram: `get_me().restricted` его не показывает,
видно только у @SpamBot. Страйк седьмой, ручной режим вернулся сам, лок до 25.09 12:46.
Урок: снимать ручной режим только после ответа @SpamBot «ограничений нет» — кнопка об этом
просит, но проверки за владельца в коде нет и быть не может (это чат владельца).
Побочный эффект: первый адресат (#23003) ушёл в карантин и получил `do_not_contact`, хотя
ограничение было на аккаунте, а не на нём.

### 23.09 вечер: почему Telegram не отправляет — ответ @SpamBot

Первая автоматическая проверка (`89cd48b`) спросила @SpamBot напрямую. Ответ: пользователи
пожаловались на сообщения как на спам, **модераторы подтвердили жалобу**; аккаунт ограничен
**до 18.10.2026 11:01 МСК** — нельзя писать тем, у кого нет номера в контактах; отвечать тем,
кто написал первым, можно; новые жалобы продлят следующий срок. `get_me().restricted` при
этом `False` — ограничение видно только у SpamBot.

Что изменено:
- Пока ручной режим, бот сам спрашивает @SpamBot каждое утро в 10:55 (и в догоне), ставит
  паузу до названной даты, до неё не спрашивает и не пробует, а на «ограничений нет» снимает
  ручной режим сам. Ответы хранятся в `account_health` (таблица была, но не заполнялась).
- Ответы рекрутёрам, которые написали сами, в ручном режиме снова уходят (`b1c4bff`), пока
  после проверки SpamBot не было PeerFlood; раньше они превращались в карточки владельцу.
- Строка Telegram на пульте в ручном режиме писала «можно сейчас» — теперь «стоит до 18.10».

Вывод: холодные сообщения в Telegram упираются не в код и не в темп, а в жалобы получателей.
До 18.10 канал закрыт; после — каждая новая жалоба удлиняет следующий срок.

### 24.09: новое резюме, готовые ответы владельца, автоответы на вопросы

- **Резюме.** Владелец прислал новое (hh.ru, Tech Lead / Backend-архитектор). Оно стоит базовым
  `cv_base/Акопян_Сурен_Python.pdf` (старое — `.2026-08-28.pdf`), 9 русских заявок в диалоге
  переключены на него; к EN-вакансиям по-прежнему уходит сгенерированное английское.
  В самом PDF противоречие: шапка «Москва, не готов к переезду», «Обо мне» — «работаю из
  Сербии, готов к релокации в Москву». Владелец подтвердил: **Москва**. PDF надо поправить.
- **faq был пуст** — любой «о себе / где вы / когда выйти / опыт в…» уходил карточкой.
  Владелец ответил (`b6fd93d`): Москва, только удалённо, переезд обсуждаем, выход сразу,
  английский C2 (было сознательно C1+), бюджет спрашиваем у них, про финтех — только
  платёжные интеграции и леджер, ссылок на работы не даём. 219 писем в очереди переведены с
  «английский C1» на C2.
- **Смелый режим не отвечал на техвопросы ни разу** (`67c74b1`): план помечал их «черновиком
  по фактам», а в задачах черновика не было `tech_question`. Тесты проверяли план, не черновик.
- **Живой прогон LLM-черновика на вопросах GDL IT** показал, что одного гейта мало: «готовы к
  офису?» → «переезд в офис обсуждаем», «финтех-интеграции»; самопроверка пропустила. Теперь
  (`249398a`) вопросы, целиком покрытые faq, отвечаются дословно текстами владельца (без LLM и
  её лимитов — все бесплатные провайдеры отдавали 429), а LLM-черновик на техвопрос режется,
  если в нём «финтех» или, при вопросе про офис, нет «только удалённо».
- Классификатор узнаёт частые формулировки («Готовы к офису?», «Когда сможете выйти?», «Есть
  опыт в…», «Where are you based?») — `07a5edf`.
- Сбор каналов (HTTP к t.me/s/, без Telethon) держал однопоточный tg-пул 30+ минут: входящие
  и ответы стояли, пульс молчал, сторож убил бы процесс. Перенесён в общий пул (`0d973e0`).
- Оживление диалогов: две попытки на тред сгорали, пока Telegram был в ручном режиме
  (ответ блокировался). Centicore просили резюме 08.09 — отправлено вручную оживлением 24.09.
- **Формат работы — по резюме владельца** (`d1ef099`, владелец: «это моё окончательное слово»):
  Москва — удалённо, гибрид или офис; к переезду не готов. Отбор больше не режет московский офис
  (`workformat.unacceptable`: офис или релокация не в Москве — отказ), письма, ответы о формате,
  faq и страховка черновиков переведены на новую позицию; 178 писем в очереди обновлены.
  Замечание: `score_job` ставит «роль профильная» даже продюсеру и HR-партнёру — вакансии режет
  только проверка базы роли в `pipeline.prepare`; раньше часть такого мусора отсекал ещё и фильтр офиса.
- **Оценка роли подтянута** (24.09, после замечания выше). Голое «продукт» в тексте давало
  «роль профильная» бьюти-дистрибутору, бренду одежды, HR-партнёру; теперь только продуктовые
  роли. Не инженерные профессии (продюсер, таргетолог, HR, FMCG) и роли вне профиля (1С,
  ПЛИС/FPGA) — мимо, если в заголовке нет инженерного слова. Роли из профиля, которые держались
  на «продукте» (CTO, CPO, менеджер продукта, технический PM, системный аналитик, QA-инженер),
  узнаются по заголовку, тегу и первой строке поста. Сравнение на боевой базе за 14 дней:
  первая версия (всё по полному тексту) пустила 51 вакансию, в основном креативные агентства,
  SMM и ассистентов; итоговая — ушло 164 (продюсеры, таргетологи, 1С, дизайн-студии), пришло
  10, все айтишные. Сужение поиска роли до одного заголовка отвергнуто: теряло 147 годных.
  Уже одобренные заявки не пересчитываются.
