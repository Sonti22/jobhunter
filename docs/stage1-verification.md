# Проверка первого этапа Jobhunter — 2026-09-06

Установлен образ `jobhunter:latest`, ID
`sha256:27e3683ca6cebbc876d4ead2739b9db7e349d0ff8b8e0c0ed83ea3f2638ac1de`.
Предыдущий рабочий образ сохранён как `jobhunter:pre-stage1-final-20260906`
(`sha256:fe82a57e74111c5037a9e555a0d45cef356dc507fa8010e86cdfae82f0ddc401`).

## Проверки

- Windows / Python 3.10: **977 passed**.
- Изолированный Docker / Python 3.12, `--network none`: **976 passed, 1 skipped**.
  Пропущен только тест Windows process API. Есть предупреждения об устаревших
  API зависимостей; ошибок тестов нет. Зависимости в этом этапе не менялись.
- Ruff, compileall, mypy для 25 модулей, проверка Git index на runtime/secret paths
  и `git diff --check`: без ошибок. Проверка индекса не проверяет историю Git.
- Отдельный полный прогон с coverage перед последним дополнением сравнений:
  976 passed, покрытие 61.07%, порог CI 50% пройден.
- Отбор проверен фиксированным корпусом `tests/fixtures/match_explanations.json`
  (81 пример), с отдельной неизменяемой фикстурой профиля.
- Проверены повторные и конкурентные нажатия, перезапуск, старые кнопки,
  истёкшие карточки, новые входящие поверх старого черновика, откат транзакции,
  приватность личного чата и отсутствие транспорта в ручном подтверждении.
- Проверены события и дедупликация, сохранение интервью после отказа,
  отделение сигнала классификатора от оффера владельца, нейтральное предпочтение
  малых выборок, история вне 90-дневного окна, неизвестная версия ручного текста.

## Миграция и сохранность

`python -m jobhunter.release_check` выполнен в Docker без сети:

- Архив старой схемы от 13:23: 11 500 заявок; контрольные суммы данных всех
  23 прежних таблиц совпали после миграции. Созданы только
  `application_feedback`, `owner_preferences`, `result_events`.
- Копия текущей базы: 11 545 заявок; 26 таблиц сохранены без изменения данных.
- На каждой копии проверены 16 веб-маршрутов/вариантов фильтра и 12 экранов бота.
  Чтение не изменило заявки, вакансии, переписку, журнал отправок, карточки и
  новые таблицы. SQLite integrity/foreign keys прошли проверку.

Непосредственно перед установкой создан и проверен архив
`backup/jobhunter-data-20260906_154308-ec8d0dc8.tgz`: 11 545 заявок,
целостность обеих SQLite-баз и контрольные суммы файлов проверены. Архив содержит
учётные данные — не публиковать. Более ранние копии не удалялись.

## После установки

Перед перезапуском не было активных отправок и ожидающих исполнения решений
отправки. `docker compose up -d --no-build --wait` завершился успешно:
`autopilot`, `bot`, `web` — healthy.

Рабочие `/ping`, `/attention`, `/reading`, `/sending`, `/results`, `/outcomes`,
`/manual-telegram` вернули HTTP 200. API подтвердили пагинацию по пять,
историю этапов и сравнения. На момент проверки: 38 дел, 84 отправленные заявки
в когорте результатов. Эти счётчики могут меняться при штатной работе.

Bot API `getMyCommands` подтвердил `tasks`, `results`, `reading`, `sending`,
`tracks`, `outreach`; `/feedback` дополнительно доступен как команда и через
кнопку причин пропуска в `/tracks`. Owner allowlist настроен.

Ручной режим Telegram сохранён, SMTP port 465 сохранён. Лимиты и блокировки
отправки не менялись. Тестовых сообщений рекрутёрам не отправляли; второй
Telegram/Telethon-клиент и второй poller бота не запускали. Живая отправка и
фактическое прочтение рекрутёром этими проверками не подтверждаются.

Работа другого помощника сохранена. Изменения этого продолжения находятся
в рабочем дереве; новый Git-коммит и push в рамках этой проверки не создавались.
